{
  "name": "target-gift-card",
  "version": "1.0.0",
  "description": "Target Gift Card Landing Page",
  "private": true,
  "scripts": {
    "dev": "npx serve .",
    "build": "echo 'No build step required for static HTML'",
    "lint": "echo 'No linter configured'"
  },
  "dependencies": {
    "@vercel/speed-insights": "^1.1.0"
  }
}
